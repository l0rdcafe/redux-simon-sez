const GAME_RESET = "GAME_RESET";
const NEXT_ROUND = "NEXT_ROUND";
const REPEAT_ROUND = "REPEAT_ROUND";

const resetGame = () => ({ type: GAME_RESET });

export { GAME_RESET, NEXT_ROUND, REPEAT_ROUND, resetGame };
